# Codex Adapter

Repository skills are discovered under `.agents/skills/`; complete workflows remain under `.ai/skills/`. Phase AI-2B validates routing and argv plans but does not run `codex exec`.

A future authorized headless run must use a read-only sandbox and ephemeral state. It must not use `danger-full-access`, `full-auto`, production access, or implicit Git writes.
