# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 依賴套件 (Windows 產生的巨大檔案，千萬不能傳)
node_modules/

# 編譯產出物
dist/
build/
.tmp/

# 環境變數與本地端快取
.env
.env.local
.DS_Store

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

.env.local
.env.*.local

.firebase/
*-debug.log
database-debug.log
storage-debug.log
travel-e2e-debug*.zip

coverage/

# AI Project OS task history is local or exported outside the repository.
.ai/tasks/archive/

# Local learning-artifact work; source JSON and rendered HTML remain tracked.
.ai/artifacts/tmp/
.ai/artifacts/drafts/

# Controlled Agent runner local policy, approvals, plans, and output.
.ai/runtime/local/
.ai/runs/

# Active agent handoff is local task state; commit reusable templates instead.
tasks/active/HANDOFF.md

# Keep active task specs visible unless intentionally archived.
!tasks/active/TASK.md
!tasks/active/.gitkeep
